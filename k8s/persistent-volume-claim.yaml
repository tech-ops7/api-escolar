apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-escolar-pvc
  namespace: gestao-escolar
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: hostpath